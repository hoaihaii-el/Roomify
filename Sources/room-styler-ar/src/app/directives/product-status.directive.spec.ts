import { ProductStatusDirective } from './product-status.directive';

describe('ProductStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new ProductStatusDirective();
    expect(directive).toBeTruthy();
  });
});
